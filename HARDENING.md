# Hardening plan — P0 (v0.2)

Gap → change → test, per P0 item. Written before the code; kept in sync as shipped. Companion: `SHIPPED-VS-DEFERRED.md` (checklist against Session‑1 basic functions and the EOI preferential criteria).

Invariants that must survive every item: `bSbi` minted once at valid receipt and never equal to or replaced by `publicRecordId`; `proposal/schemas/events.ts` byte-identical to the app copy; outbox append-only; `reconcile()` clean; `replay-outbox` inserts nothing on a consistent DB; home page stays functions-first.

Schema goes to **v4** (no migrations — `db:reset`). All additions are implementation columns/tables; the Zod contract is untouched (see `CONTRACT-AMENDMENTS.md` C6–C8).

| # | Gap (v0.1) | Change (v0.2) | Test (`npm run smoke`) |
|---|---|---|---|
| 1 | `requireCan()` guards every mutation, but a refusal only becomes `?error=` on a redirect — nothing is recorded; no role × action matrix test. | `access_refusals` table (append-only). `requireCan(p, action, subject?, ctx?)` records `{actor_role, actor_user_id, action, domain, record_id, path, reason}` before throwing. Pages that hard-gate and export routes call `recordRefusal()` on 403. `/audit` shows a **Refusals** panel in the Secretariat projection only. | Matrix `[anonymous, public, party, stb, secretariat] × [submit, publish, comment_stb, import, suggest_match, view_full_audit, amend, export_full]` against a table of expected booleans; each refused `requireCan` leaves exactly one refusal row; public projection never exposes refusals. |
| 2 | Dispatcher emits `publish/deadline/stb_review/match` after commit, but the `digest` cadence on subscriptions is ignored; the only `digest` row is hand-inserted by the seed. | **Hold semantics.** Subscription-matched `publish` targets are split by cadence: `immediate` → row now; `daily`/`weekly` → held (no row). Owner `publish`, `deadline`, `stb_review`, `match` stay immediate. `src/server/digest.ts` `runDigests(db, now)` rolls held events since the user's last window into one `digest` notification and a `digest_runs` row. `npm run digest`; Secretariat-only "Run digest now" on `/notifications`. Seed calls `runDigests` instead of inserting a row. | Publish an MGR pack matching `party.nfp` (daily): no per-event bell; `runDigests` → exactly one digest row naming it; second run → 0 inserts; `public` (immediate, CCZ) still receives per-event; `replay-outbox` still 0. |
| 3 | Template + bounded import exist; results live in a 6 kB querystring; no durable run, no error report a Party can correct and resubmit, no import audit. | `import_runs` table; `importMgrExcel` returns `runId`; action redirects to `/mgr/import/[runId]`. Run page lists accepted rows (→ pending pack pages) and rejected rows with field-level errors, plus **Download error report (.xlsx)** = same template, Data sheet holding only the failed rows + `Error` column, so the Party fixes and resubmits. Fixture gains one deliberately invalid row. Runs appear in the Secretariat audit projection. | Fixture → N‑1 accepted, 1 rejected; error workbook re-imports after correction → accepted; `import_runs` count; error workbook carries the `Error` column and the template marker. |
| 4 | Versions are server-side and `v+1` after publish exists, but there is no way to amend a *published* pack with a reason, no material-change flag, no version history, and amendments do not re-notify earlier readers. | `events.change_note`, `events.material_change`. `amendPack()` in `packs.ts`: latest pack of (record, stage) must be `published`; opens `pending v+1` carrying note + flag; MGR pre-collection amendments also write the edited Art 12.2 fields and append the previous values to `mgr_batches.details_history_json`. On publish of `v>1`, dispatch summary reads "Amended vN (material change): …" and, when material, additionally targets every user holding a notification for an earlier version of that (record, stage). Version-history panel + amend form on record pages. Policy action `amend`. | publish v1 → amend material → v2 pending → publish v2: each v1 recipient gets one `publish` row for v2; non-material amendment notifies subscribers only; amending a non-published stage refused; `bSbi`/`publicRecordId` unchanged; `reconcile()` clean. |
| 5 | Tiers are enforced in SQL for lists/audit/notifications, but no `confidential` seed exists, no tier × role matrix, no explanation in views, CBTMT cannot set a tier. | Seed a confidential MGR batch and a restricted EIA activity. Tier selector on CBTMT create. `ConfidentialityBadge` explains who can see the record (derived from `readPolicy`). Every export goes through the same policy queries. | Matrix `[public, restricted, confidential] × [anon, public, party‑owner, party‑other, stb, secretariat]` over `listMgrBatches`, `recordVisible`, `packsOf`, `auditRows`, `recentPublished`, `resolvePublicRecord`, `notificationsFor`, export functions. |
| 6 | No export. | `src/server/export.ts`: CSV (RFC 4180) and JSON envelope `{schemaVersion, generatedAt, role, count, rows}` for `mgr/eia/cbtmt/audit`, reusing the policy-filtered queries. Routes `/api/export/[domain].csv|json`, `/api/records/[prid].json`, `/api/records/[prid].pdf` (hand-rolled one-page text PDF, labelled stub). Buttons on list, audit and record pages. | CSV header equals column list; anonymous export excludes restricted rows; secretariat export includes drafts; unknown/invisible record → 404 and a refusal row; PDF begins `%PDF-1.4` and ends `%%EOF`. |
| 7 | `/compare` is a six-row static table without links or mapping to the webinar's functions. | Rewrite around Session‑1 basic functions (receipt/management/storage · notification · user management) plus cross-cutting rows (tiers, versioning, export, offline loop, refusal audit). Every row deep-links into the live seeded journey. Footnotes map to consolidated study ¶61 and Art 51.5. | Page renders for anonymous and secretariat (HTTP check in `npm run demo` when `BASE_URL` set). |
| 8 | `db:reset` is CLI-only; no unattended demo path; no HTTP-level checks. | In-process `resetSandbox()`; Secretariat-only **Reset sandbox** button shown only when `SANDBOX_RESET=1` outside production (or `=force` for a hosted sandbox image). `scripts/demo.ts` (`npm run demo`) runs the full 10‑minute journey on a temp DB with narrated checkpoints; with `BASE_URL` it also fetches pages/exports with each demo cookie and asserts status codes. Smoke grows to cover all of the above. | `npm run smoke` all green; `npm run demo` exits 0. |

## Non-goals (unchanged)

Production OAuth/SMTP, federation, Solr, ABS chain, ML matching, forking ABSCH, GIS EIA. ABMT stays reserved unless P1 buffer allows an empty shell.
